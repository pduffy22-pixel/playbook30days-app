[build]
  command = "npm run build || test -f .output/public/index.html"
  publish = ".output/public"

# Client routes (intake, playbook, meal, moves, vitamins)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
